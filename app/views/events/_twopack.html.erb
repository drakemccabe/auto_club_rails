<div class="col-sm-4 price-block">
  <h3><%= @two_pack.dates.last.strftime("%b #{@two_pack.dates.last.day.ordinalize}") %></h3>
  <div class="price pastel-green">
    <div class="price-figure">
      <h4><%= @two_pack.dates.last.strftime("%a") %></h4>
    </div>
  </div>
  <ul class="features">
    <li>$<%= first_price %> - <%= registration.capitalize %> Registration Price </li>
    <li><%= render "form", amount: first_price * 100, event_id1: @two_pack.ids.last, event_id2: nil, name: @event.name %></li>
    <li><p class="small">Note: The email you enter will be used for communication if an event is canceled or rescheduled. Club Loose North respects your privacy and will not distribute your email or use it for marketing.</p></li>
    <li>
      <p>
        <% if registration == 'early' %>
          Early Registration ends on <%= time.strftime("%a, %b #{time.day.ordinalize} at %l:%M %P") %>
          Same week registration costs $<%= first_price + 15 %> and ends at 4 AM the day before the event.
        <% elsif registration == 'same week' %>
          Same week registration ends on <%= time.strftime("%a, %b #{time.day.ordinalize} at %l:%M %P") %>
        <% end %>
      </p>
    </li>
  </ul>
</div>

<div class="col-sm-4 price-block">
  <h3><%= @two_pack.dates.first.strftime("%b #{@two_pack.dates.first.day.ordinalize}") %> and <%= @two_pack.dates.last.strftime("%b #{@two_pack.dates.last.day.ordinalize}") %></h3>
  <div class="price pastel-green">
    <div class="price-figure">
      <h4><%= @two_pack.dates.first.strftime("%a") %> and <%= @two_pack.dates.last.strftime("%a") %></h4>
    </div>
  </div>
  <ul class="features">
    <li>$<%= price %> - <%= registration.capitalize %> Registration Price</li>
    <li>
       <%= render "form", amount: price * 100, event_id1: @two_pack.ids.first, event_id2: @two_pack.ids.last, name: @two_pack.name %>
    </li>
    <li>
      <p class="small">Note: The email you enter will be used for communication if an event is canceled or rescheduled. Club Loose North respects your privacy and will not distribute your email or use it for marketing.</p>
    </li>
    <li>
      <p>
        <% if registration == 'early' %>
          Early Registration ends on <%= time.strftime("%a, %b #{time.day.ordinalize} at %l:%M %P") %>
          Same week registration costs $<%= @two_pack.price + 15 %> and ends at 4 AM the day before the event.
        <% elsif registration == 'same week' %>
          Same week registration ends on <%= time.strftime("%a, %b #{time.day.ordinalize} at %l:%M %P") %>
        <% end %>
      </p>
    </li>
  </ul>
</div>


