<%= @event.name %>
<%= @event.price %>
<%= @event.location %>

<%= render "form", amount: "1500", label: " $15 ", event_id1: @event.id, event_id2: nil %>

<% unless @two_pack.nil? %>
  <%= render "form", amount: "1500", label: " $15 ", event_id1: @two_pack.ids.first, event_id2: @two_pack.ids.last %>
<% end %>
